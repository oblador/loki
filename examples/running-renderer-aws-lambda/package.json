{
  "name": "@loki/example-running-renderer-aws-lambda",
  "version": "0.35.0",
  "private": true,
  "scripts": {
    "build": "sh build.sh",
    "start": "docker run -d -p 8080:8080 -t example-renderer-aws-lambda:latest"
  },
  "workspaces": {
    "nohoist": [
      "**"
    ]
  },
  "dependencies": {
    "@loki/renderer-aws-lambda": "^0.35.0",
    "@sparticuz/chromium": "118.0.0"
  }
}
